# Enrich using https://www.maturin.rs/metadata.html
[project]
name = "cdshealpix"
requires-python = ">=3.8"
dependencies = ["numpy>=1.18", "astropy>=5.1"]

# Build a cdshealpix-x.x.x.tar.gz containing sources (from maturin).
[build-system]
requires = ["maturin>=0.13"]
build-backend = "maturin"


